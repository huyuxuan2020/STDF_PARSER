### 解析问题说明更直观

- 文件存在记录长度或字段长度问题时，概览会直接对比“文件中实际是”和“正常情况下应为”，并解释为什么会报错。
- 能够确认记录边界时，会显示原始字节、被误读的内容、正确的下一条记录位置和正确记录头，不再只显示难以理解的 REC_LEN 数字。
- 分开显示错误根因数量和受影响记录数量，避免将一次边界错误误报成大量独立错误。

### 诊断准确性改进

- 边界错位后产生的 UNKNOWN、未闭合器件或程序段等连锁现象会合并到根因中，不再作为独立问题重复显示。
- 相同错误的实际值不一致时，会明确标注第一处示例，并保留对应位置，避免用第一处内容代表所有错误。
- 精确边界判断已覆盖更多变长字符串字段；证据不足时会明确说明无法可靠判断，不会猜测或修改原文件。
- 只有提醒时使用黄色提示；存在会影响解析或统计的错误时才使用红色提示。
