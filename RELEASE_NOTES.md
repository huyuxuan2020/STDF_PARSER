### ✨ 新功能

- **支持更多压缩包**：新增 7z、rar、tar、xz 等常见格式，加上原有的 zip 和 gz，平时收到什么包基本都能直接拖进来。包里混了别的文件也没关系，会自动找到里面的 STDF。
- **概览页良率统计**：解析完直接看良率、Pass/Fail 数量和 SBIN/HBIN 分布；复测晶圆按每颗 die 的最终结果算，不会重复计数。
- **DTR 文本导出**：明细页选中 DTR 记录，可以把整份文件的 DTR 文本提取出来存成 TXT。

### ⚡ 性能

- **解析明显变快**：尤其是大文件，打开基本不用干等了。
- **磁盘占用大幅下降**：临时索引不再动辄几十 GB，打开新文件时也会自动清理旧的。
- **浏览不再卡顿**：翻页、查字段、按方向键连续看记录都是即时响应，测试项矩阵再大也能顺滑滚动。

### 🎨 界面

- **整体视觉优化**：布局、配色、字号、动效重新梳理了一遍，深色模式也更耐看。

### 🐛 修复

- Mac 自带「压缩」出来的 zip 之前打不开，现在可以了。
- 压缩包里 macOS 的隐藏元数据文件（._xxx、__MACOSX）不再被误当成 STDF。
- 文件已打开时再拖入新文件，偶尔报「解析会话不存在」，已修复；报错横幅现在也能手动关掉。
- 表格内滚动到底，不会再把整个页面带着一起滚。
