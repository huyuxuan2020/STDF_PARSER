### 新功能

- 概览页新增“文件解析提示”：仅在发现问题时显示，并放在良率信息上方。
- 报错内容改为更易理解的说明，包含问题是什么、是否影响统计结果、建议如何处理，以及对应的文件位置。
- 相同问题会自动汇总并显示出现次数，同时保留最多 3 个位置示例，避免大量重复信息干扰查看。

### 解析诊断

- 新增文件头、记录长度、字段截断、额外数据、未知记录、版本与字节序等格式检查。
- 新增测试流程完整性检查，包括批次、晶圆、器件测试和程序段的开始/结束配对。
- 新增管脚引用、时间、测试上下限以及异常数值检查。
- 发现非致命问题后仍会继续解析可读取的内容，不会自动修改文件或猜测数据边界。
- 当大量未知记录表明文件边界可能已经错位时，会明确提示后续结果可能不准确，并抑制由错位引发的重复连锁报错。
